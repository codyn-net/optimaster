### Optimaster configuration file

### The discovery namespace to use
# discovery namespace =

### The discovery address to use
# discovery address =

### The address on which to listen for tasks
# listen address =

### The maximum number of task failures before rejecting a job
# max task failures = 5

### The number of tasks to estimate the run time over
# runtime estimation = 10

### The command password, if not set authentication will always fail
# command password =
