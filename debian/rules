#!/usr/bin/make -f

makebuilddir/optimaster::
	./autogen.sh

include /usr/share/cdbs/1/rules/debhelper.mk
include /usr/share/cdbs/1/class/autotools.mk

DEB_MAKE_INSTALL_TARGET = install prefix=$(CURDIR)/debian/tmp/usr
